export enum NodeEnv {
    TEST = `test`,
    DEVELOPMENT = `development`
}

export enum ResponseMessage {
    SUCCESS = `Success`,
    DOES_NOT_EXIST = `Sku does not exists`,
}

export enum ResponseCode {
    SUCCESS = 200,
    BAD_REQUEST = 400
}

export enum LoggerMessages {
    API_CALLED = `Api Has Been Called.`,
}